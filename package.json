{
  "name": "slack-proxy-server",
  "version": "1.0.0",
  "description": "Proxy server to send reports to Slack with a cooldown mechanism.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "node-fetch": "^2.7.0"
  }
}
